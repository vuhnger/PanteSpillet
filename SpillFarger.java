import java.awt.Color;

public abstract class SpillFarger {
    public static final Color
    RUTE_KANT = Color.BLACK,
    TEKST = new Color(99,50,0), // Brun
    BAKGRUNN = new Color(246, 192, 55), // Gul
    TOM = BAKGRUNN,
    KROPP = TEKST,
    PANT = Color.RED;
}
